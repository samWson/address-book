apply plugin: 'java'

jar {
    manifest {
        attributes (
                'Main-Class' : 'Main',
                'Implementation' : project.name,
                'Developer' : 'samWson'
        )
    }
}

